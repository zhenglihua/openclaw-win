<Page x:Class="OpenClawManager.Views.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:local="clr-namespace:OpenClawManager.Views"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="800"
      Title="仪表盘">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0"
                   Text="仪表盘"
                   FontSize="28"
                   FontWeight="SemiBold"
                   Margin="0,0,0,24"/>

        <!-- 状态卡片 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- OpenClaw状态 -->
            <ui:Card Grid.Column="0" Margin="0,0,8,0">
                <StackPanel>
                    <ui:SymbolIcon x:Name="OpenClawIcon" Symbol="CheckmarkCircle24"
                                  FontSize="32"
                                  Foreground="{Binding OpenClawStatusColor}"/>
                    <TextBlock Text="OpenClaw"
                              FontWeight="SemiBold"
                              Margin="0,8,0,4"/>
                    <TextBlock Text="{Binding OpenClawStatusText}"
                              Foreground="Gray"
                              FontSize="12"/>
                </StackPanel>
            </ui:Card>

            <!-- 网关状态 -->
            <ui:Card Grid.Column="1" Margin="8,0,8,0">
                <StackPanel>
                    <ui:SymbolIcon x:Name="GatewayIcon" Symbol="Globe24"
                                  FontSize="32"
                                  Foreground="{Binding GatewayStatusColor}"/>
                    <TextBlock Text="网关"
                              FontWeight="SemiBold"
                              Margin="0,8,0,4"/>
                    <TextBlock Text="{Binding GatewayStatusText}"
                              Foreground="Gray"
                              FontSize="12"/>
                </StackPanel>
            </ui:Card>

            <!-- 端口状态 -->
            <ui:Card Grid.Column="2" Margin="8,0,8,0">
                <StackPanel>
                    <ui:SymbolIcon x:Name="PortIcon" Symbol="Connector24"
                                  FontSize="32"
                                  Foreground="{Binding PortStatusColor}"/>
                    <TextBlock Text="端口"
                              FontWeight="SemiBold"
                              Margin="0,8,0,4"/>
                    <TextBlock Text="{Binding PortStatusText}"
                              Foreground="Gray"
                              FontSize="12"/>
                </StackPanel>
            </ui:Card>

            <!-- 服务状态 -->
            <ui:Card Grid.Column="3" Margin="8,0,0,0">
                <StackPanel>
                    <ui:SymbolIcon x:Name="ServiceIcon" Symbol="Server24"
                                  FontSize="32"
                                  Foreground="{Binding ServiceStatusColor}"/>
                    <TextBlock Text="服务"
                              FontWeight="SemiBold"
                              Margin="0,8,0,4"/>
                    <TextBlock Text="{Binding ServiceStatusText}"
                              Foreground="Gray"
                              FontSize="12"/>
                </StackPanel>
            </ui:Card>
        </Grid>

        <!-- 快捷操作 -->
        <ui:Card Grid.Row="2" Margin="0,24,0,0">
            <StackPanel>
                <TextBlock Text="快捷操作"
                          FontWeight="SemiBold"
                          FontSize="16"
                          Margin="0,0,0,16"/>

                <WrapPanel>
                    <ui:Button Content="启动服务"
                              Icon="{ui:SymbolIcon Play24}"
                              Appearance="Primary"
                              Command="{Binding StartServiceCommand}"
                              Margin="0,0,8,8"/>

                    <ui:Button Content="停止服务"
                              Icon="{ui:SymbolIcon Stop24}"
                              Command="{Binding StopServiceAsyncCommand}"
                              Margin="0,0,8,8"/>

                    <ui:Button Content="重启服务"
                              Icon="{ui:SymbolIcon ArrowSync24}"
                              Command="{Binding RestartServiceCommand}"
                              Margin="0,0,8,8"/>

                    <ui:Button Content="打开Web UI"
                              Icon="{ui:SymbolIcon WindowNew24}"
                              Command="{Binding OpenWebUICommand}"
                              Margin="0,0,8,8"/>

                    <ui:Button Content="运行诊断"
                              Icon="{ui:SymbolIcon Stethoscope24}"
                              Command="{Binding RunDiagnosticCommand}"
                              Margin="0,0,8,8"/>
                </WrapPanel>
            </StackPanel>
        </ui:Card>
    </Grid>
</Page>
